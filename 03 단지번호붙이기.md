## 단지번호붙이기
- https://www.acmicpc.net/problem/2667
~~~python
n = int(input())
houseList = [list(map(int, input())) for _ in range(n)]

dx, dy = [0, 0, -1, 1], [1, -1, 0, 0]
totalHouse = 0 
houseCount = []

def dfs(houseCount, x, y):
    houseList[x][y] = 0
    houseCount[-1] += 1
    for i in range(4):
        nx = x + dx[i]
        ny = y + dy[i]
        if 0 <= nx < n and 0 <= ny < n:
            if houseList[nx][ny] == 1:
                dfs(houseCount, nx, ny)
                
                
for i in range(n):
    for j in range(n):
        if houseList[i][j] == 1:
            houseCount.append(0)
            dfs(houseCount, i, j)
            totalHouse += 1
            
print(totalHouse)
for i in sorted(houseCount):
    print(i)
~~~

### 문제풀이
깊이 우선 탐색으로 1로 표시된 주택 단지들을 방문처리(0) 하며 단지 수와 주택수를 count 한다.
