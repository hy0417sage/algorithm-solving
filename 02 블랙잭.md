## 블랙잭 
- https://www.acmicpc.net/problem/2798

~~~python
from itertools import permutations

N, M = map(int, input().split())
cards = list(map(int, input().split()))

per_cards = [i for i in map(sum, permutations(cards, 3)) if i <= M]

print(max(per_cards))
~~~

### 문제풀이
카드의 개수가 3~100개 사이로 모든 경우의 수를 계산해 M 보다 작거나 같은 값중 최대값을 계산
