## 연구소
- https://www.acmicpc.net/problem/14502

~~~python
from itertools import combinations
import copy
n, m = map(int, input().split())
graph = [list(map(int, input().split())) for _ in range(n)]
result = 0
dx, dy = [0, 0, -1, 1], [-1, 1, 0, 0]

def wallCase(): #벽을 세울 수 있는 케이스 64*63*62
    global graph
    case = []
    for i in range(n):
        for j in range(m):
            if graph[i][j] == 0:
                case.append([i, j])
    return list(combinations(case, 3))
   
def virus(x, y):
    temp[x][y] = 2
    for i in range(4):
        nx = x + dx[i]
        ny = y + dy[i]
        if 0 <= nx < n and 0 <= ny < m:
            if temp[nx][ny] == 0:
                virus(nx, ny)
    
def safe():
    safe_count = 0
    for i in range(n):
        for j in range(m):
            if temp[i][j] == 0:
                safe_count += 1
    return safe_count
  
for case in wallCase(): #벽 세우고
    temp = copy.deepcopy(graph)
    for i in range(3): 
        x, y = case[i]
        temp[x][y] = 1
    for i in range(n): #바이러스 퍼트리고
        for j in range(m):
            if temp[i][j] == 2:
                virus(i, j)
    result = max(result, safe())
        
print(result)
~~~

### 문제풀이 - dfs와 조합으로 풀이
- 벽을 세우고 -> 바이러스를 터트리고 -> 안전지역을 계산한다. 방식으로 진행
- 벽을 세울 수 있는 케이스를 combinations으로 전체를 구하고 (64 * 63 * 62 = 249,984 여서 가능이라고 생각함)
- 깊은 복사로 temp를 따로 둔 다음 여기서 바이러스를 터티리고 안전지역을 계산했다.
- 안전지역이 가장 큰 결과를 반환
