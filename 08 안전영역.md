## 안전영역
- https://www.acmicpc.net/problem/2468

### 풀이 - 실패(오늘 다 실패하네..)
1. 물에 잠기게 한다. 100 * 100
2. 안전영역을 계산한다. 100 * 100 * 4
3. 1 + 2 = 50000 * 100 = 5000000 최악의 경우 시간복잡도 계산이 이렇게 나와서 가능할 것이라고 생각했다.(조건을 잘못 준거 같기도..)
두가지의 조건을 세우고 1부터 100까지 반복해 계산하려 했다.
~~~python
import copy

n = int(input())
graph = [list(map(int, input().split())) for _ in range(n)]
temp = [[False] * n for _ in range(n)]

dx, dy = [0, 0, -1, 1], [-1, 1, 0, 0]

def water(number): #물에 잠기게 하기 100 * 100 = 10000
    result = 0
    for i in range(n):
        for j in range(n):
            if graph[i][j] <= number:
                visited[i][j] = True #물에 잠기게 하기
                
    for i in range(n):
        for j in range(n):
            if visited[i][j] == False: #물에 안 잠긴 곳
                dfs(i, j)
                result += 1
                
    return result
   

def dfs(x, y): #안전영역 계산하기 10000 * 4 = 40000
    visited[x][y] = True
    for i in range(4):
        nx = x + dx[i]
        ny = y + dy[i]
        if 0 <= nx < n and 0 <= ny < n:
            if visited[nx][ny] == True:
                dfs(nx, ny)
result = 0 
visited = copy.deepcopy(temp)
for i in range(1, 101):
    visited = copy.deepcopy(temp)
    result = max(result, water(i))
    
print(result)
~~~
