## 미로 탐색
- https://www.acmicpc.net/problem/2178
~~~python
from collections import deque

n, m = map(int, input().split())
graph = [list(map(int, input())) for _ in range(n)]

dx, dy = [0, 0, -1, 1], [1, -1, 0, 0]

def bfs(x, y): 
    q = deque([(x, y)])
    
    while q:
        x, y = q.popleft()
        
        for i in range(4):
            nx = x + dx[i]
            ny = y + dy[i]
            if 0 <= nx < n and 0 <= ny < m:
                if graph[nx][ny] == 1:
                    graph[nx][ny] = graph[x][y] + 1
                    q.append((nx, ny))
                    
bfs(0, 0)
print(graph[n-1][m-1])
        
~~~
### 문제풀이 - bfs
위치로 이동해야하는 최소 칸의 수를 구할 것, bfs로 시작 지점부터 넓이 우선 탐색. 상하좌우로 이동하며 전 칸에서 +1 을 함으로 n,m자리까지 최소 횟수를 계산할 수 있음
