## 다리만들기
- https://www.acmicpc.net/problem/2146

#### 실패 - 틀림
1. visited를 따로 만들어서 섬을 인식하게 하고
2. 땅일 경우 visited 를 깊이 우선 탐색으로 False -> True로 만든다음
3. bfs 넓이 우선 탐색으로 
  - 육지에서 다른 육지로로 왔을 경우
  - 이미 계산된 다리 길이가 존재할 경우
  - 바다일 경우
  세가지 조건을 만들고 계산 되도록 함
~~~python
from collections import deque
n = int(input())
graph = [list(map(int, input().split())) for _ in range(n)]

visited = [[False] * n for _ in range(n)]
dx, dy = [0, 0, -1, 1], [-1, 1, 0, 0]

def dfs(x, y): #조건 추가: 육지를 검사할 때
    visited[x][y] = True
    for i in range(4):
        nx = x + dx[i]
        ny = y + dy[i]
        if 0 <= nx < n and 0 <= ny < n:
            if graph[nx][ny] == 1 and visited[nx][ny] == False:
                dfs(nx, ny)
                
def bfs(x, y):
    q = deque([(x, y)])
    
    while q:
        x, y = q.popleft()
        for i in range(4):
            nx = x + dx[i]
            ny = y + dy[i]
            if 0 <= nx < n and 0 <= ny < n:
                if graph[nx][ny] == 0:
                    graph[nx][ny] = graph[x][y] + 1
                elif graph[nx][ny] == 1 and visited[nx][ny] == True: #육지에서 다른 육지로
                    return graph[x][y] - 1 
                elif graph[nx][ny] != 0 and graph[nx][ny] != 1 and visited[nx][ny] == False: #이미 계산 된 케이스
                    return graph[nx][ny] - 1
                q.append((nx, ny))
                
result = []                
for i in range(n):
    for j in range(n):
        if visited[i][j] == False and graph[i][j] == 1:
            dfs(i, j)
            result.append(bfs(i, j))

print(min(result))
            
~~~
